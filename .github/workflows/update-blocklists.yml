name: Update own lists

on:
  push:
    branches:
      - main
    paths:
      - 'lists/**'

  workflow_dispatch:

concurrency:
  group: blocklists-push
  cancel-in-progress: false

jobs:
  update-lists:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
          ref: main

      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
          ref: blocklists
          path: blocklists

      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24.x
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Configure Git
        run: |
          git config --global user.name "Sefinek Actions"
          git config --global user.email "sefinek.actions@gmail.com"

      - name: Deduplicate lists/ and push to main
        run: |
          node scripts/pipeline/dedupe.js lists

          git add lists/

          if [ -z "$(git status --porcelain)" ]; then
            echo "No changes in lists/ to commit."
          else
            FILE_COUNT=$(git diff --name-only --cached | grep -c . || true)
            git commit -m "Deduplicate lists/ [$FILE_COUNT files] [skip ci]"
            git push origin main
          fi

      - name: Apply whitelist to templates
        run: node scripts/pipeline/whitelist.js

      - uses: ./.github/actions/process-and-commit
        with:
          commit_message: "Update own lists (${{ github.sha }})"
